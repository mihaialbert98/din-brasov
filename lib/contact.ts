/**
 * Public contact addresses for the platform.
 *
 * Currently a plain Gmail while a domain mailbox isn't set up yet. When
 * contact@dinbrasov.com / dpo@dinbrasov.com receiving is configured (forwarding
 * or Google Workspace), swap the values here — every page/email updates.
 */
export const SUPPORT_EMAIL = "contact@dinbrasov.com";
export const DPO_EMAIL = "dpo@dinbrasov.com";

/**
 * Official social media profiles. Single source of truth — used by the footer,
 * homepage hero, About page and the JSON-LD `sameAs` list. Add a new network
 * here and wire it into those spots.
 */
export const INSTAGRAM_URL = "https://www.instagram.com/din_brasov/";
export const FACEBOOK_URL = "https://www.facebook.com/dinbrasov";
